/**
 * 帮助页面内容（应用内嵌版，更完整的说明见随包附带的《使用说明.md》文档）
 */
'use strict';
export const HELP_HTML = `
<h2>快速上手</h2>
<ol class="steps">
  <li><b>配置数据源</b>：「设置」页填入 Alpaca API Key（<a href="https://alpaca.markets/" target="_blank">免费注册</a>，行情数据用免费的 IEX feed 即可，不需要付费订阅）。未配置时系统会自动降级用 Yahoo Finance 免费接口，但拿不到长历史小时线。</li>
  <li><b>选一个股票池</b>：「股票池」页点「载入」，建议先用 Industry Universe（几十只，扫描快）熟悉流程，再尝试 Core Universe（500+只，耗时较长，注意免费数据源限流）。</li>
  <li><b>运行扫描</b>：「扫描」页点「开始扫描」，结果按综合评分从高到低排列，点代码或「详情」查看三频共振/机构多因子/评分明细的完整拆解。</li>
  <li><b>生成派生池</b>：「派生池」页一键从刚才的扫描结果里筛出 Momentum / Quality / High RS / New Highs / CANSLIM候选 / Minervini候选 / 机构买入代理池。</li>
  <li><b>历史回溯（核心功能）</b>：「历史回溯」页输入股票代码 + 任意历史日期，系统会把所有价格类指标截断到那一天重新计算，还原当天真实的信号状态，不是"最新数据贴个历史标签"。</li>
</ol>

<h2>关于数据的诚实原则</h2>
<p>本系统的所有免费数据源都有各自的局限，遇到拿不到的数据会明确显示"数据不可用"，绝不会编造数字或用最新数据冒充历史数据：</p>
<ul>
  <li>基本面/机构持仓数据（Quality层、CANSLIM的C/A/I子项）：免费Finnhub额度只提供最新快照，历史回溯时会标注"⚠️非时点数据"。</li>
  <li>小时线数据：只有配置 Alpaca Key 才能拿到真实小时线；否则"短周期"信号会自动降级为"短期日线替代"，界面会标注清楚用的是哪种。</li>
  <li>Institutional Buying 机构买入池：免费数据源没有真实13F持仓变化数据，本池用"持续放量+资金流向上"作为代理指标，明确标注不代表真实机构调仓。</li>
  <li>RS 相对强度百分位：是相对"本次扫描样本"的排名，不是IBD/MarketSmith那种全市场RS Rating（那需要全市场历史数据基础设施）。</li>
</ul>

<h2>历史日期回溯说明</h2>
<p>如果你输入的日期是周末或节假日，系统会自动使用之前最近的一个交易日，并在结果里标注实际使用的日期。批量回溯（对一整个股票池同一天重新计算）比单只查询多了"RS百分位在该批样本内有统计意义"这个额外价值。</p>

<h2>云端定时扫描</h2>
<p>把整个项目 push 到你自己的 GitHub 仓库，配置好 Secrets 后，<code>.github/workflows/daily-scan.yml</code> 会在每个交易日收盘后自动跑一次扫描并把结果提交回仓库；「设置」页填入你仓库的 raw 内容地址后，「云端结果」页就能读到历史记录，不需要一直开着浏览器。</p>

<h2>模块化架构一览</h2>
<pre>js/
├─ data/        数据层：Alpaca客户端、Yahoo兜底、统一门面、基本面、静态代码列表
├─ core/        核心：指标计算、时间周期转换、Universe引擎、分析流水线、
│               扫描引擎、历史回溯引擎、风控工作台、通知、云同步
├─ signals/     信号引擎：三频共振、机构多因子(Minervini/Weinstein/CANSLIM/RS)、
│               市场环境、组合评分器、质量评分
└─ ui/          界面控制器（本文件所在目录）
</pre>
<p>想替换/新增指标，只需要改 <code>core/indicators.js</code>；想调整信号规则，只需要改 <code>signals/</code> 目录下对应文件；不需要理解或改动其他任何模块。</p>
`;
